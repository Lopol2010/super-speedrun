#if defined _checkpoints_included
	#endinput
#endif
#define _checkpoints_included

native checkpoint(id);
native gocheck(id);
native get_checkpoints_count(id);
native get_gochecks_count(id);
native reset_checkpoints(id);
